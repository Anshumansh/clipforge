import type { Metadata } from "next";

export const metadata: Metadata = { title: "Script to video" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
